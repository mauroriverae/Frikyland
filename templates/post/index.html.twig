{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}

    {{form(form)}}
    {{dump(posts)}}
    {# {{dump(custom_post)}} #}
    {# {{dump(posts)}}
    {{dump(search)}}
    {{dump(oneBy)}} #}
{% endblock %}
