{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}

    {{form(form)}}
    {# {{dump(post)}} #}
    {# {{dump(custom_post)}} #}
    {# {{dump(posts)}}
    {{dump(search)}}
    {{dump(oneBy)}} #}
{% endblock %}
